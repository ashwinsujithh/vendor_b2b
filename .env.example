# Copy this file to .env and adjust the values
PORT=3002

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
# Use connect_vendors after importing `connect_vendors (2).sql`.
# The bundled demo setup uses storepanel instead.
DB_NAME=connect_vendors

# Use a long random string in production
JWT_SECRET=change-me-to-a-long-random-string
